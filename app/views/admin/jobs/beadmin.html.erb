<div class="col-md-12">

  <div class="group">
      <%= link_to("New User", new_admin_job_path, class: "btn btn-primary pull-right") %>
  </div>


  <h2 class="text-center">Admin Panel</h2>

  <table class="table">
    <thead>
      <tr>
        <th>#</th>
        <th>Title</ht>
        <th>Description</th>
        <th>Last Update</th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td>#</td>
          <td><%= user.email %></td>
          <td><%= user.is_admin %></td>

          <td>

               <%= link_to("Edit", edit_admin_job_path(job) , class: "btn btn-sm btn-default") %> |
               <%= link_to("Destroy", admin_job_path(job),     class: "btn btn-sm btn-default", method: :delete, data: {confirm: "Are you sure?"}) %>

          </td>
          <td><%= job.updated_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
